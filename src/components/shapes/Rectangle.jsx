const Rectangle = ({ color }) => {
  return (
    <div
      style={{
        backgroundColor: color,
        height: "50px",
        width: "100px",
        margin: "5px",
      }}
    ></div>
  );
};

export default Rectangle;
