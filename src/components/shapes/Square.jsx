const Square = ({ color }) => {
  return (
    <div
      style={{
        backgroundColor: color,
        height: "50px",
        width: "50px",
        margin: "5px",
      }}
    ></div>
  );
};

export default Square;
